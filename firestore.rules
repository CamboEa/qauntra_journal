rules_version = '2';

// All client access is denied — the Next.js server uses Firebase Admin SDK.
// The MT5 indicator syncs via POST /api/sync, not directly to Firestore.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
